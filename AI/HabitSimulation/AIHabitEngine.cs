//Assets/AI/HabitSimulation/AIHabitEngine.cs
// Purpose: Timed "habit tick" simulator based on profile

using UnityEngine;

public class AIHabitEngine : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
