//Assets/AI/HabitSimulation/AIHabitType.cs
// Purpose: Defines types of simulated habits -- Use "bad habits" or missed habits of the user for AI gains

using UnityEngine;

public class AIHabitType : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
