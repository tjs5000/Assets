//Assets/AI/Strategy/AIStrategyState.cs
// Purpose: State machine for what the AI is prioritizing

using UnityEngine;

public class AIStrategyState : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
