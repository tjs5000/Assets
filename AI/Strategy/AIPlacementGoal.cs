//Assets/AI/Strategy/AIPlacementGoal.cs
// Purpose: Declares targets (e.g. "increase beauty score")

using UnityEngine;

public class AIPlacementGoal : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
