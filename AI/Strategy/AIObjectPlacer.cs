//Assets/AI/Strategy/AIObjectPlacer.cs
// Purpose: Places park objects based on income, visitor needs, game strategy and/or game goals

using UnityEngine;

public class AIObjectPlacer : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
